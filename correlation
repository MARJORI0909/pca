## Correlaci?n M?ltiple

### Librer?as

library(corrplot)

### Base de datos ajustada

tarwi2.cor <- tarwi2[,5:10]

par(mfrow=c(1,1))

i <- cor(tarwi2.cor, method = "pearson")

corrplot(i, method = "color",order = "AOE", type = "upper", tl.pos = "d")
corrplot(i, add = TRUE, type = "lower", method = "number", order = "AOE",
         diag = FALSE, tl.pos = "n", cl.pos = "n")

}






























#CORRELACIONES combinaciones de variables 
ggplot(peg, aes(x=pH, y=CE, color=PEG)) + 
  geom_point(size=6) +
  theme_ipsum() + xlab("pH") + ylab("CE (mS/cm)")

ggplot(peg, aes(x=Peso_final, y=CE, color=PEG)) + 
  geom_point(size=6) +
  theme_ipsum() + xlab("Peso turgente (g) ") + ylab("CE (mS/cm)")

ggplot(peg, aes(x=Peso_final, y=pH, color=PEG)) + 
  geom_point(size=6) +
  theme_ipsum() + xlab("Peso turgente (g)") + ylab("pH")

ggplot(peg, aes(x=Peso_final, y=CRA, color=PEG)) + 
  geom_point(size=6) +
  theme_ipsum()+ xlab("Peso turgente (g)") + ylab("CRA (%)")

ggplot(peg, aes(x=Peso_final, y=IMB, color=PEG)) + 
  geom_point(size=6) +
  theme_ipsum()+ xlab("Peso turgente (g)") + ylab("IMB(%)")

ggplot(peg, aes(x=CRA, y=pH, color=PEG)) + 
  geom_point(size=6) +
  theme_ipsum()+ xlab("CRA (%)") + ylab("pH")

ggplot(peg, aes(x=CRA, y=CE, color=PEG)) + 
  geom_point(size=6) +
  theme_ipsum()+ xlab("CRA(%)") + ylab("CE (mS/cm)")

ggplot(peg, aes(x=IMB, y=pH, color=PEG)) + 
  geom_point(size=6) +
  theme_ipsum()+ xlab("IMB (%)") + ylab("pH")

ggplot(peg, aes(x=IMB, y=CE, color=PEG)) + 
  geom_point(size=6) +
  theme_ipsum()+ xlab("IMB (%)") + ylab("CE (mS/cm)")

ggplot(peg, aes(x=IMB, y=CRA, color=PEG)) + 
  geom_point(size=6) +
  theme_ipsum()+ xlab("IMB (%)") + ylab("CRA (%)")
